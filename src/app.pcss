/* Write your global styles here, in PostCSS syntax */
@tailwind base;
@tailwind components;
@tailwind utilities;

:root {
    --dark-bg: #1a1a1a;
    --dark-text: #d9d9d9;
}

body {
   @apply text-lg font-sans text-[var(--dark-text)] bg-[var(--dark-bg)];
}
